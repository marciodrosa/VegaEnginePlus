require "vegatable"

--- The C api to comunicate with the C app. It is dinamically created by the App C++ class when instantiated.
-- This table is used internally. It isn't needed to use this table if the default main loop is used with the App C++ class.
vega.capi = {
}
